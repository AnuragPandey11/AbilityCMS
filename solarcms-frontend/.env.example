# Base URL for the REST API. Leave as /api to use the Vite dev proxy.
VITE_API_BASE=/api
# WebSocket origin. Leave empty to derive from window.location.
VITE_WS_BASE=
# Only used by vite.config.ts when proxying in development.
VITE_API_TARGET=http://localhost:8000
